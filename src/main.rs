#![allow(dead_code, unused_variables, unreachable_code, unused_assignments)]
mod a_variables;
mod b_basic_types;
mod c_ownership_and_borrowing;

// The main() fn is the starting point of every rust program
fn main() {}
